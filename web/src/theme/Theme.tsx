import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `'Crawlify-heading', sans-serif`,
    body: `'Crawlify-body', sans-serif`,
  },
});

export default theme;
